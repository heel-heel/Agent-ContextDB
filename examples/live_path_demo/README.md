# Live Path Skill Demo

This fixture supports the live ContextDB path-repair demonstration.

The intentional failing lookup is `./assets/settings.json` from the project
root. The real fixture is stored at `./examples/live_path_demo/assets/settings.json`.
The fixture is static and does not modify ContextDB runtime data.
